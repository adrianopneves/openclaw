Nenhum agente chamado "Pola" foi encontrado no repositório OpenClaw. O nome mais próximo encontrado é `@polarbear-Yang`, que é um **contribuidor externo** mencionado no `CHANGELOG.md` (linha 872) que submeteu um PR de correção para Azure OpenAI Responses — não um agente do sistema.

---

**TITLE:** Agente "Pola" no OpenClaw — Não Encontrado

**EXEC_SUMMARY:**
Não existe nenhum agente chamado "Pola" definido ou referenciado no ambiente OpenClaw. O único match próximo ao nome é um contribuidor externo (`@polarbear-Yang`), não um agente funcional.

**KEY_FINDINGS:**
- Nenhuma definição de agente "Pola" encontrada em `AGENTS.md`, `.agents/`, ou qualquer outro arquivo do repo
- `@polarbear-Yang` é um contribuidor de PR (CHANGELOG.md:872), não um agente
- Agentes definidos no repo vivem em `.agents/` e `.agent/` — nenhum deles corresponde a "Pola"

**NEXT_STEPS:**
- Verifique se "Pola" é um agente configurado externamente (ex: via `~/.openclaw/agents/`) com `ls ~/.openclaw/agents/`
- Ou confirme se o nome correto é outro (ex: um alias ou apelido de algum agente existente)

**FULL_ANALYSIS:**
A busca cobriu todo o repositório `/Users/adrianon/openclaw` incluindo `AGENTS.md`, `.agents/maintainers.md`, CHANGELOG, e código-fonte. Nenhum agente, skill, ou componente com o nome "Pola" existe. É possível que o agente esteja configurado fora do repositório (em arquivos de config locais do OpenClaw) ou que o nome seja diferente do esperado.
